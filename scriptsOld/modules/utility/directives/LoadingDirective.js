hcUtility.directive('loading',[function() {
    
    var tpl = 'dffdfg';
    
  return {
  	restrict : 'E',

  	// link: function(scope, e, attr){  		
  	// 	scope.isWatched = function(model){
  	// 		return model.watched == 0 ? 'mark watched'  : 'mark unwatched';
  	// 	};
  		
  		

  		
  	},
  	template: tpl
  };  
}]);